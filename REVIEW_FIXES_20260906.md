# V0.2.1 预览与框选修复（2026-09-06）

基于 main 的 `784eeec`，已对照桌面 `Pokemmo孵蛋助手V0.2-修复版-20260905` 的修复说明与 EXE 校验值。旧桌面版本和实际素材库存未修改。

## 本次修改

- 双击放大预览默认等比例适合窗口，居中完整显示；窗口改变尺寸时重新计算，不再仅初始化一次，也不再限制放大到 200%。不同比例会有留边，不拉伸或裁切游戏信息。
- 修复实时帧用主窗口旧 ROI 覆盖弹窗未应用选框的问题。清除、默认区域、鼠标框选和坐标编辑都有独立的待应用状态。
- 拖动时暂缓画面重绘，固定本次手势的坐标映射；松开后处理最新帧。使用实际渲染位图的横纵比例，避免缩放取整造成位置偏移。主窗口和弹窗均已处理。
- 允许自由框选至少 2×2 原图像素的矩形；小于阈值的误点恢复旧区域。新增 X、Y、宽、高的原图坐标微调，非法或越界输入不会被应用。
- 工具栏按查看、选区、坐标分行；显示缩放比例、选区尺寸、待确认状态，底部明确区分“应用框选并收回”和“取消并收回”。滚轮以指针位置为缩放锚点。
- 切换截图或捕获来源时关闭旧来源的待应用选区；双击打开弹窗时清理主窗口未结束的鼠标手势。
- 发布文件固定为 `Pokemmo孵蛋助手.exe`，ZIP 默认版本 V0.2.1。同步修订 Windows OriginalFilename、使用说明和哈希清单文件名；保留程序内作者信息及版权字段。

## 验证

- 全量单元回归：222 项通过，其中 22 项新增覆盖预览几何、实时帧、坐标编辑和主窗口交互。
- 真实 Tk 布局断言：1000×720、640×460、1400×900 三种窗口尺寸全部通过；画面恰好占满可用区的一个维度且不裁切，所有按钮和坐标输入框完整显示。手动 1:1 在调整窗口大小后保持不变，恢复适合窗口正常。
- Windows UI 检查：使用 `scripts/preview_smoke.py` 的模拟实时画面和临时隔离库存，通过实际鼠标双击、框选、等待多次刷新、应用回主窗口，验证区域没有被旧框覆盖。检查了扫描和规划页面。边缘拖拽的 UI 自动化未成功改变窗口大小，因此尺寸适配以真实 Tk 布局断言作为验证依据。
- 最终单文件 EXE：正常启动到主界面；`--check-ocr` 离线模型初始化退出码 0。测试进程使用独立数据目录，不接触实际库存。
- ZIP：CRC 检查通过；四个条目为 EXE、使用说明、第三方声明、SHA256；包内 EXE 哈希与独立 EXE、清单一致，没有 `-晨若` 文件名。
- 最终 EXE SHA-256：`7f343015db6ae8d22453fa08793b259d3b150ff4e583dcce6f7727106b8ae638`。
- 本次没有连接游戏或执行真实精灵扫描；游戏内不同 UI 缩放、主题、布局变化后的选区仍需用户核对。

## UI 后续建议（未改变现有流程）

1. 顶部工作区与下方页签都出现“孵蛋规划”，导航层级重复。后续可统一为扫描、库存、规划三个主入口，但需要一并梳理小窗口布局和返回路径。
2. 规划页目标表单与多条状态栏占用较多纵向空间。已有收起功能；后续可合并空状态说明，让首次生成路线后优先展示路线图。
3. 窗口列表会默认选中首个可见窗口，未必是游戏。后续可提供明确的“请选择窗口”占位，或在多候选时提示用户确认，减少误选其他窗口。

## 复现入口

- `python -m unittest discover -s tests`（需安装依赖或将 vendor 加入 PYTHONPATH）
- `python scripts/check_preview_layout.py`（需要可用的桌面和 Tk）
- `python scripts/preview_smoke.py`（手动 UI 验证，无游戏连接，临时库存）
- `./release.ps1 -Python <Python 3.10–3.12 路径>`（输出 `release/Pokemmo孵蛋助手V0.2.1.zip`）
