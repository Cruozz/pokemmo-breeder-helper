# PokeMMO_EggCalc_v4.0 参考工具拆解

审查日期：2026-08-24

参考目录：`D:\LNS\Desktop\PokeMMO_EggCalc_v4.0`

本报告只记录对本地文件的只读分析。没有读取 PokeMMO 进程、内存或客户端数据，也没有修改参考工具的数据库。

## 1. 内容目录

```text
PokeMMO_EggCalc_v4.0/
├─ PokeMMO_EggCalc_v4.0.exe       # PyInstaller Windows 程序，约 2.9 MB
├─ pokemmo.db                     # SQLite 数据库，约 40 KB
├─ 全地区精灵分布.xlsx             # 五个地区的出现地点数据
├─ 技能遗传链.xlsx                 # 遗传技能链数据
└─ _internal/
   ├─ github_open_tool/
   │  └─ VGC-Damage-Calculator-Chinese/  # 捆绑的通用伤害计算器
   ├─ images/                            # stone.png 等界面资源
   ├─ myplot.png                         # 绘图/界面资源
   ├─ PIL/                               # Pillow 运行库
   ├─ _tk_data/、_tcl_data/、tcl8/       # Tk/Tcl 运行库
   ├─ Python 3.9、SQLite、OpenSSL 等 DLL
   └─ 其它 Windows/Python 运行库文件
```

总文件数约 1,217 个，其中绝大多数是 PyInstaller、Python、Tk/Tcl 和时区运行库。主程序源码不在普通目录中，而是嵌在 EXE 的 PyInstaller 归档内：主模块名为 `pokemmo_1`，核心 Python 模块包括 `Database`、`constants`、`heart`、`damage_engine` 和 `damage_ui`。

## 2. 核心功能目录

### 2.1 孵蛋规划与库存优先求解

`heart` 模块是核心求解器，包含：

- `BreedingNode`：孵蛋树节点；
- `BreedingSolver.solve`：求解目标；
- `build_stat_tree`：按个体值构建多代金字塔/树；
- `build_nature_chain`：处理性格链和不变石；
- `find_inventory`：从库存寻找可用素材；
- `InventoryFirst`：优先消耗已有库存；
- 性别、无性别、百变怪、主蛋组/副蛋组匹配；
- `lock_nature`、`lock_final_gender`：锁定性格和最终性别；
- `get_gender_selection_fee`：估算性别选择费用；
- `prices1`、`prices2`、`prices_ditto`：素材价格/费用估算；
- `used_inventory_counts`、`apply_plan_to_inventory`：统计和应用库存消耗。

它的结果不只是“一对父母”，而是包含中间合成步骤、库存使用量、新需购买素材、狗圈/不变石等费用的完整孵蛋树。

### 2.2 数据查询

主模块中有：

- `load_poke_distribution_data`：加载全地区分布表；
- `get_dist_pokemon_candidates`：按精灵查询分布；
- `load_skill_chain_data`：加载遗传技能表；
- `get_skill_pokemon_candidates`：按精灵和技能查询遗传来源；
- `open_search_window`：查询结果窗口。

### 2.3 价格、攻略和外部链接

界面还提供了几个与孵蛋计算相关但不属于核心求解器的模块：

- 可编辑的蛋组/个体值/性别素材价格表；
- 32 个攻略槽位，可以保存文本攻略或图片攻略；
- 28 个外部链接槽位，支持链接名称、链接地址和二维码显示；
- 留言/备注区；
- 攻略搜索、画布缩放、拖动画布和节点高亮；
- 独立的孵蛋统计摘要窗口。

### 2.4 通用伤害计算器

`damage_engine` 和 `damage_ui` 是附加模块，另有完整的 `VGC-Damage-Calculator-Chinese` HTML/JS/JSON 资源。它用于通用宝可梦伤害范围计算，与 PokeMMO 仓库识别和孵蛋路线没有直接关系。

## 3. SQLite 数据模型

参考数据库包含 6 张表：

| 表 | 主要字段 | 当前状态 |
|---|---|---|
| `inventory` | `egg_group`, `gender`, `stats`, `count`, `nature` | 0 条 |
| `material_prices` | `egg_group`, `stat`, `gender`, `price` | 180 条默认价格 |
| `guides` | `slot`, `name`, `file_path`, `file_type` | 32 个空槽 |
| `links` | `slot`, `name`, `url` | 28 个空槽 |
| `notes` | `id`, `content` | 1 条空备注 |
| `sqlite_sequence` | SQLite 内部序列 | 空 |

`material_prices` 是 15 个蛋组 × 6 项能力 × 公/母两种价格键。默认值中普通蛋组为 5,000，无性别蛋组为 60,000。参考程序把库存设计成“按蛋组、性别、能力组合聚合的数量”，而不是逐只精灵记录。

## 4. 两个工作簿的数据规模

### 4.1 全地区精灵分布.xlsx

- 工作表：丰缘、关都、合众、神奥、城都，共 5 张；
- 有效数据约 4,443 行；
- 约 567 个不同精灵；
- 表头主要是：`路线、精灵、出现、稀有度、备注、努力值、组别、几率携带`；
- 记录了草丛、水面、钓鱼、山洞、撞树、特殊草丛、时间段、香水/稀有度等地点条件；
- `组别`列存在空格、别名、旧称和少量明显录入异常，例如“人形了”“水1足虫组”“矿组乌”等，需要规范化后再进入规划器。

### 4.2 技能遗传链.xlsx

- `Sheet1` 有效数据约 11,145 行；
- 约 269 个目标精灵；
- 约 173 个遗传技能；
- 表头为：`宠物、技能、遗传链路`；
- `Sheet2`、`Sheet3` 为空；
- 一个技能可能对应多个来源链路，一个精灵也可能有很多可遗传技能。

## 5. 值得移植到当前工具的内容

按优先级建议如下：

1. **库存优先的多代路线摘要**：保留当前工具逐只素材记录的优势，同时增加“每一步用哪两只、消耗什么、产出什么、还缺什么”的右侧摘要面板。
2. **可选的价格/费用估算**：价格只作为附加排序和成本参考，不改变“尽量使用已有素材”的主目标，并且全部允许用户修改。
3. **更完整的路线统计**：显示库存消耗数、需要新准备的各项素材、狗圈/不变石/性别选择次数和总费用。
4. **分布与技能查询的可视化结果**：当前工具已有静态数据和查询入口，可以吸收它的候选列表、筛选和来源显示方式。
5. **备注/攻略链接**：可以作为低优先级的辅助页，保存本地攻略、说明和外部链接，但不应影响 OCR、库存和规划主流程。

## 6. 不建议照搬的部分

- **聚合库存模型**：它只保存数量、蛋组、性别、能力组合和性格，无法追踪单只精灵的仓库位置、OCR 来源、置信度、技能、头目属性或核销历史。当前工具的逐只 `Monster` 记录更适合实际核销。
- **默认价格**：5,000/60,000 只能作为参考，不能当作当前市场事实；应使用可编辑的本地价格配置，并明确“路线目标仍是库存优先”。
- **原始蛋组字符串直接参与计算**：参考工作簿有不少空格、别名和疑似错字。当前工具应继续使用规范化蛋组和别名表，同时保留原始来源文本供复核。
- **捆绑伤害计算器**：体积和维护成本较高，与本项目核心需求无关；除非之后明确要做战斗分析，否则暂不加入。
- **依赖当前工作目录的打包方式**：直接启动参考 EXE 时出现 `unable to open database file`。这说明 EXE、`pokemmo.db` 和 `_internal` 的资源定位/启动工作目录存在兼容问题。当前工具应统一使用明确的数据目录和用户可写的本地应用数据目录。

## 7. 与当前工具的边界结论

静态模块和资源中没有发现 OCR、PokeMMO 进程读取、内存读取、自动鼠标键盘输入或自动翻页模块。参考工具本质上也是“用户手动提供/导入数据，程序在本地计算”，只是它更偏向聚合数量和价格驱动的孵蛋计算器；当前工具更偏向可追踪的 OCR 素材仓库和逐步核销。

因此后续可以吸收它的“求解器结果表达、库存优先评分、费用统计、攻略查询组织方式”，但继续保留本项目的底线：只处理可见截图、OCR 和本地静态资料，不读取 PokeMMO 数据，不代替用户操作游戏。
