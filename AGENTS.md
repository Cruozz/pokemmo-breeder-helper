# 项目协作约定

## 必须执行的发布要求（用户确认：2026-09-15）

每次完成一批源码修改，默认必须同时完成 Windows 打包和 GitHub 上传；不能只交付本地源码。用户在当次任务明确要求不发布时除外。

1. 使用与构建依赖匹配的 Python 3.10–3.12，优先 3.12。vendor 是解释器 ABI 相关目录，不要把 3.12 vendor 混入 3.13 测试环境。
2. 更新 app.py 的 APP_VERSION、version_info.txt、发布说明和使用说明；使用新的语义版本，不覆盖已经公开发布的版本和资产。
3. 运行完整测试、语法检查及 git diff --check；失败时修复，不得带失败结果发布。
4. 使用 PowerShell 7 执行 release.ps1。它须构建单文件 Windows EXE、执行 EXE 自检、生成 ZIP 和 SHA-256。测试实际产物，不以源码测试代替打包验证。
5. 检查 git diff，确保未包含真实库存、账号数据、密钥、vendor、构建缓存或无关文件。
6. 将本批源码提交并推送到 origin（预期 Cruozz/pokemmo-breeder-helper）的 main；推送前确认远端未出现未整合的新提交。禁止强推、覆盖别人提交或重复发布旧版本。
7. 建立 v<版本号> 标签和 GitHub Release，至少上传版本 ZIP、单文件 EXE、SHA256SUMS.txt；发布前核对账号和仓库写入权限。尽量先创建 draft 并验证所有资产，再公开。
8. 核对 GitHub 上的提交、标签、资产尺寸和哈希；回复中给出发布页、下载地址、本地文件及测试结果。
9. 如果网络、凭据或构建阻塞，保留已完成工作，明确区分“已打包”“已推送源码”“已公开 Release”，不得把未完成的上传声称为完成。

这是针对后续源码修改的持续约定，不是定时任务。不要为了满足该约定擅自操作游戏或上传用户库存。
