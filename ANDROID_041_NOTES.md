# 安卓版 0.4.1

## 本轮修复

- 使用用户提供的原图制作应用图标及开屏。开屏按原比例显示，图片下方为“让孵蛋更简单”，约 1 秒后进入，也可点击进入；Android 12+ 的系统启动页使用同一图标。
- 目标性格改为可搜索的完整选择列表，包括 25 种性格、不指定和无修正。支持输入中文片段或英文名；输入“天真”后可点确定，点同名候选不会再把值清空。
- 不指定排最前，带星号的固执、内敛、爽朗、胆小紧随其后，无修正在最后。
- 新建目标的六项个体值全部默认为 X，每项下拉选择 0 / X / 31。已有启用路线的目标会保留，不擅自修改存档。
- 大字体小屏改用两列个体值；窄高度窗口的性格弹窗收起标题，为搜索和候选列表留出空间。
- 去掉 Android 路线节点的重复整理；对图鉴模糊匹配和纯函数结果使用有容量上限的缓存。
- 同一配对分支的公素材候选只整理一次，仍逐对检查库存冲突，保持 0.2.2 的孵蛋和排序规则，不缩小搜索宽度。

## 性能核对

使用 tools/benchmark_mobile.py，在同一电脑、同一 Python 3.12 环境，以优化前源码备份对比。测试均为合成数据，不读写真实库存。

| 5V 天真索罗亚克 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 空库存 | 1.388 秒 | 0.442 秒 |
| 120 只模拟库存 | 13.461 秒 | 6.500 秒 |

两组路线报告的 SHA-256 均与优化前一致。耗时会随设备负载和素材组合变化；这些不是手机实测秒数，也不是全部路线的性能保证。

## 兼容与备份

沿用相同包名与调试签名，可覆盖安装。升级前先导出库存；不要通过卸载或清除数据解决安装问题。

本轮保留 0.4.0 APK，并备份了升级前全部非忽略源码。没有修改用户真实库存，也没有提交或推送 GitHub。

图标和开屏素材由用户提供；本轮不对该图的商用许可作额外声明。

## 验收结果

- 247 项 Python unittest、1 项独立桥接测试及 2 项 Kotlin 单元测试已通过。
- 最终 11 项模拟器测试全部通过，覆盖性格搜索和确认、原值保持、0/X/31 选择、开屏图片和标语，以及规划核销和存档恢复。
- 已实际查看开屏、小屏默认全 X 表单、性格列表及横屏叠加 2 倍字体的暗色弹窗截图；仍未完成真实手机、平板和全部路线页面的视觉验收。
- 最终 APK 为 release/PokeMMO-Breeder-Helper-Android-v0.4.1-debug.apk，版本号 0.4.1 / versionCode 5，Android 8.0+，arm64-v8a 和 x86_64。
- 签名验证通过，与保留的 0.4.0 APK 证书一致；构建内共享 Python 模块与当前源码哈希一致。
