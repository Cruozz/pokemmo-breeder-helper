# 安卓 0.4.5：启用前预览路线与筛选素材

- 生成建议后直接展示思维导图，可切换完整步骤清单，无需先确认启用。
- 点击预览中的真实库存素材节点，可选择“本轮禁用并重算”；也可展开“本轮素材”列表查看个体值、账号及位置后筛选。
- 连续禁用会累积，重算后的路线排除这些素材。“恢复全部并重算”清除本轮禁用；即使未生成新路线，也保留恢复入口。
- 有原执行路线时，可切换“预览建议 / 执行路线”进行比较，或放弃建议保留原路线。
- 预览节点和步骤标记为未启用，不能核销或记录孵化结果。禁用、恢复与重算不修改库存或已启用路线；确认启用后才保存新方案。
- 保留上一版规划提速、路线身份颜色、遗传技能、阶段续接、撤销及清除规划功能。

## 安装

`PokeMMO-Breeder-Helper-Android-v0.4.5-debug.apk`：Android 8.0+，arm64-v8a / x86_64，versionCode 9，沿用原调试签名，可覆盖安装。

配套 Windows V0.2.11 更新版本与说明，孵蛋核心保持 V0.2.10。

## 验证

- 323 项 Python 回归测试、1 项独立桥接测试及 56 个 Python 文件语法检查通过。
- 4 项 Kotlin 单元测试、20 项 Android 模拟器测试通过，新增首次建议预览、列表禁用、连续禁用、恢复重算、原路线保留及启用保存测试。
- 在 Android 模拟器中实际检查未启用导图、素材节点禁用按钮及重算后的恢复入口；使用合成库存，未操作用户真实库存。
- Windows 单文件 EXE 实际自检通过；APK 版本及与上一版相同的签名已核对。
