# V0.2.2 执行路线一致性修复与验证

## 修复内容

- 将正在执行的路线和新建议预览分开。执行图由保存的步骤及子代依赖直接构建，不再借用新建议的节点编号；只读预览不能核销、撤销或标记正在孵化。
- 核销确认显示实际执行方案、目标、步骤及素材。符合计划的实际性别继续原路线，不再仅因记录了随机性别就重新规划。
- 母体、直接辅助素材和配偶性别明确的节点按配对需求锁定；旧方案可根据已存在的配偶补足锁定策略。
- 结果不符合计划或需要切换性格阶段时保存真实子代并暂停，询问是否生成调整建议；建议不会自动替换正在执行的路线。启用前检查素材是否仍在库存。
- 完成子代后默认折叠父母及更早来源，可双击展开。历史素材标为已消耗，不重新加入库存；保存原始材料快照，便于重启后查看历史。
- 保留原目标、性格阶段与规划选项，避免新建议的表单设置覆盖实际任务。修复宽路线图初始水平定位，使根节点可见。
- 发布版本升级至 0.2.2；ZIP 内应用名仍为 `Pokemmo孵蛋助手.exe`。

## 2026-09-07 复核结果

- 完整自动测试：240 项通过（本轮 13.624 秒）。覆盖旧/新方案隔离、性别一致/不一致/取消、完成来源折叠、暂停状态保存、旧格式兼容、过期建议拒绝及路线居中等场景。
- 原生界面：使用 `scripts/execution_smoke.py` 的合成精灵和临时数据库，确认执行→只读建议→执行切换保留进度；只读模式核销/撤销不可用；完成来源默认折叠，可展开查看已消耗父母，再次折叠正常。
- 最终 EXE：在隔离的 LOCALAPPDATA 目录正常启动；`--check-ocr` 返回 0。
- ZIP：CRC 检查通过；归档内 EXE 与版本目录 EXE 完全一致；SHA256.txt 与实际哈希一致；归档文件名不含 `-晨若`；EXE 文件版本为 0.2.2。
- EXE SHA-256：`aa87dd6caf3d4342e02103377190b25e7b992c353e347c6e57a60311e673336b`。
- `git diff --check` 通过；合成路线界面测试错误日志为空。

## 数据边界

本次修改和复核没有操作游戏，没有修改用户真实库存及执行记录，也没有恢复用户已消耗的 3V 公圆丝蛛。已在游戏内完成的 5V 飞天螳螂是否录入软件仍以用户实际库存记录为准，不根据截图自动补造记录。测试核销仅使用合成数据及隔离存储。
