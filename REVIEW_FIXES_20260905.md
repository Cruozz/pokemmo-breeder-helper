# V0.2 桌面审查修复 · 2026-09-05

## 修复范围

1. 逐级性格手阶段先检查实际 IV 包含关系、性别、蛋组与父母 ID，避免有素材但无法配对时进入空的合流阶段；缺少合适的下一档素材时继续制作可合流的性格手。双蛋组采购保底保留各蛋组选项。
2. 普通 OCR 入库仅以蓝钻检测结果自动标记梦特，不再以特性名称推断；头目的既有规则、手动修改保持不变。不批量改写旧库存。
3. 候选路线保存保留母体/性格手的实际库存 ID，支持手动/旧 JSON 入库素材。核销撤销快照包含这些引用。刚完成的素材优先保留，已排除或丢失的素材不偷偷替换成另一只。
4. 独立性格手和母体主线不再被描述为直接进化成最终目标；最终进化提示验证真实进化路径和性别。按 UI/UX 状态反馈原则区分各阶段，不改界面布局。
5. 性格手搜索按每种 IV 组合的实际母本及可配对库存机会排序，然后保留有限搜索宽度和交易行后备，避免仅按种类名称截断。仍是有界搜索，不承诺所有库存规模下的数学全局最优。

补充保护：独立性格手阶段禁止全目标百变直配捷径消耗已保留母体；尚未执行的公转母分支不能当作已完成母体。

## 数据与兼容

- 没有数据库迁移，没有修改真实库存、核销历史、扫描聚焦行为。
- 旧候选快照缺少新增字段时仍能加载；旧快照显示保留素材时沿用原有历史匹配后备。
- 原有 README 与 Android 未提交工作保持不动；本次未提交或上传 GitHub，未重新打包 APK。
- 桌面修复版单独输出到 release/V0.2-修复版-20260905，不覆盖旧 V0.2。

## 验证

使用项目自带 Python 3.12：

最终全套 200 项测试通过（27.848 秒），其中本轮新增 13 个回归测试方法；git diff --check 通过。
独立修复版 EXE 构建成功，`--check-ocr` 包内 OCR 初始化诊断退出码为 0。
未对真实游戏进行交互测试。旧程序、真实 inventory.db 的 SHA-256 在修复前后保持一致。

```
.\.runtime\python312\python.exe -m unittest discover -s tests -p "test_*.py"
```

新增 tests/test_revision_regressions.py，覆盖不匹配 IV、跨蛋组、库存截断、蓝钻、保留素材、快照、4V/5V 阶段、百变保护、临时数据库内完整逐级保底与撤销。

额外复现：备份的 852 条库存 + 模拟 5V 非目标性格头目梦特尼多兰，规划得到 3 条 gamble_upper 路线；本机一次运行约 1.4 秒，3 条路线的当前性格手均为 1 次孵化、0 采购。

测试使用临时数据库或读取库存副本。模拟用例和自动化检查不等同于用户电脑的游戏实操验收，尤其尚未验证实际游戏截图下的 OCR 与窗口交互。

## 备份

F:\Codex\pokemmo-backups\before-five-fixes-20260905-145959

内含源码、旧发布包、库存与执行计划，以及恢复说明.md。回退程序不需要回滚库存；数据恢复前务必另存当前数据。

## 2026-09-06 源码上传安排

- 修复源码上传到 `upgrade/v0.2-fixes-20260905`，暂不替换 GitHub 的 `main`。
- `backup/github-main-20260906` 保留上传前的 GitHub main。
- `backup/desktop-before-fixes-20260906` 保留本轮桌面修复之前的 V0.2 提交。
- 两份备份均不删除；手机版的其他未提交改动不包含在本次桌面修复提交中。
