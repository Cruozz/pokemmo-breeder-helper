# 安卓版 0.4.0

## 规则与操作

- 桌面规则基线：V0.2.2 / e4bee8a。构建直接同步共享 Python 模块，不另写 Android 孵蛋算法。
- 导入电脑库存 JSON 后离线规划；可按账号筛选素材。
- 生成建议不会自动替换正在执行的路线，确认启用后才保存。
- 完成步骤需确认实际结果；采购素材无需扫描入库。正常完成保留原路线 ID；父母核销、中间代入库、最终成品不入素材库存。
- 仅随机性别步骤要求记录公母；下一步与百变怪配对且性别无关时不询问。性格判断档位与逐级性格手沿用共享规则。
- 改变路线的结果会保留实际子代并暂停旧路线，可按原目标生成下一阶段建议，再确认启用。
- “孵化中”只是备注，不核销，也不解锁依赖。
- “撤销上次操作”整体恢复库存和路线；再次点击可恢复撤销前状态。导入和启用路线也属于操作，不仅是核销。

## 存档与界面

- 库存和路线统一原子保存到 workspace-v2.json，防止只保存其中一半；保留旧 inventory.json / plan-session.json 文件用于兼容迁移。
- 存档读取失败会阻止覆盖；请勿卸载或清除数据。
- 旧版仅打勾的路线只能查看，需重新生成并启用 0.2.2 规则路线才能核销。
- 支持导出手机当前库存回电脑；这是库存 JSON，不包含手机完整路线和撤销快照。
- 性格可输入搜索，常用性格优先；确认弹窗可滚动，操作按钮换行，已完成节点默认折叠来源。
- 路线说明是初始方案报告，当前进度以步骤和暂停提示为准。

## 安装和验证

- Android 8.0+，arm64-v8a / x86_64；本轮交付 debug 签名测试 APK，不是应用商店正式发布包。
- 安装前建议导出库存。优先覆盖安装；如签名不兼容，不要直接卸载导致存档丢失。
- 已通过 246 项 Python unittest、1 项单独桥接测试、6 项 Android 模拟器测试及 Kotlin 单元测试。
- 模拟器测试覆盖包内规划和核销、确认必选项、百变配对免问性别、原子存档重启和整体撤销。
- 已检查小屏表单，以及横屏叠加 1.5 倍字体、暗色模式的初始页面；窄高度窗口收起标题区。尚未完成真实手机、平板、最大字体及全部路线页面的视觉验收，不宣称全机型适配或规划全局最优。
- 本轮保留旧 APK，未提交或推送 GitHub，未修改用户真实库存。
